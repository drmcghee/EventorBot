// async listStep(step) {
    // // remember the state slected
    // step.values.state = step.result.value;

    // var asyncFunction = util.promisify(eventorClient.loadData)
    // var fromDate  = "2019-07-18"
    // var toDate = "2019-07-30"
    // console.log("sending message")
    // result = await asyncFunction(`/api/events?fromDate=${fromDate}&toDate=${toDate}`)
    //     .then((text) => {
    //         console.log('CONTENT:', text);
    //         return
    //     })
    //     .catch((err) => {
    //         console.log('ERROR:', err);
    //         return err
    //     });

    
               // // Xml to JSON object
               // var xml2js = require('xml2js');
               // var parser = new xml2js.Parser({explicitArray : false});
               // parser.parseString(xml, function (err, json) {
               //     if (err)
               //         reject(err);
               //     else
               //         resolve(json);
               // });